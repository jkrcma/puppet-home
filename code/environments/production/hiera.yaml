---
version: 5

hierarchy:
  - name: "Per-node data"
    path: "%{trusted.certname}.yaml"
  - name: "Per-OS defaults"
    path: "%{facts.os.family}.yaml"
  - name: "Common data"
    path: "common.yaml"
